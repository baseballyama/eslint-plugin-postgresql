---
"eslint-plugin-postgresql": minor
---

Add `postgresql/prefer-timestamptz` rule: warns on `timestamp without time zone` columns and recommends `timestamptz`. Enabled at `warn` severity in `configs.recommended`.
