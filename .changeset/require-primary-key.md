---
"eslint-plugin-postgresql": minor
---

Add `postgresql/require-primary-key` rule: warns on `CREATE TABLE` statements that do not declare a primary key. Enabled at `warn` severity in `configs.recommended`.
