errors:
  - messageId: noUpdatePk
    message: Avoid `UPDATE` on the primary-key column `id`. Primary keys are intended to be immutable; FK references and external systems can hold the old value.
    line: 1
    column: 18
    endLine: 1
    endColumn: 20
  - messageId: noUpdatePk
    message: Avoid `UPDATE` on the primary-key column `orders_id`. Primary keys are intended to be immutable; FK references and external systems can hold the old value.
    line: 2
    column: 19
    endLine: 2
    endColumn: 28
output: null
