errors:
  - messageId: noVolatileDefault
    message: "`random()` is VOLATILE — using it as a column DEFAULT on `ADD COLUMN` forces PostgreSQL to rewrite the entire table under `ACCESS EXCLUSIVE`. Add the column without a default, then `UPDATE` rows in batches."
    line: 1
    column: 36
    endLine: 1
    endColumn: 43
  - messageId: noVolatileDefault
    message: "`gen_random_uuid()` is VOLATILE — using it as a column DEFAULT on `ADD COLUMN` forces PostgreSQL to rewrite the entire table under `ACCESS EXCLUSIVE`. Add the column without a default, then `UPDATE` rows in batches."
    line: 2
    column: 34
    endLine: 2
    endColumn: 41
output: null
