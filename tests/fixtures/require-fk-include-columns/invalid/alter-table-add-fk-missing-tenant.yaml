options:
  - columns:
      - tenant_id
errors:
  - messageId: missingFkColumn
    message: Foreign-key constraint on `orders` references `users` but does not include `tenant_id`. In a multi-tenant database every FK should carry the tenant key so a child row cannot point at a parent in a different tenant.
    line: 3
    column: 7
    endLine: 4
    endColumn: 41
output: null
