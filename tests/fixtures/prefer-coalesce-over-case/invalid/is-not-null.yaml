errors:
  - messageId: preferCoalesceOverCase
    message: "`CASE WHEN ... IS NULL THEN ... ELSE ... END` is a verbose `COALESCE`. Use `COALESCE(x, fallback)` instead."
    line: 1
    column: 8
    endLine: 1
    endColumn: 12
output: null
