options:
  - max: 10
errors:
  - messageId: identifierTooLong
    message: Identifier `looong_table` is 12 bytes, which exceeds the 10-byte limit. PostgreSQL silently truncates over-length identifiers at parse time, so the object will be created (or looked up) under a different name than written — every later `DROP` / `ALTER` / `\d` that uses the original name will then fail with `does not exist`.
    line: 2
    column: 14
    endLine: 2
    endColumn: 26
output: null
