options:
  - style: never
errors:
  - messageId: unexpectedBetween
    message: Use `x >= 1 AND x <= 10` instead of `BETWEEN`. Some teams prefer explicit comparisons so the inclusive bounds are obvious to readers.
    line: 1
    column: 16
    endLine: 1
    endColumn: 34
output: SELECT 1 WHERE x >= 1 AND x <= 10;
