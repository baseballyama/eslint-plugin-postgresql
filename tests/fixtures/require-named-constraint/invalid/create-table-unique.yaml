errors:
  - messageId: requireNamedConstraint
    message: Table-level CHECK / UNIQUE / FOREIGN KEY / EXCLUSION constraints should be named with `CONSTRAINT <name>`. Auto-generated names are unpredictable across environments and make later `DROP CONSTRAINT` / `ALTER CONSTRAINT` migrations brittle.
    line: 1
    column: 40
    endLine: 1
    endColumn: 46
output: null
