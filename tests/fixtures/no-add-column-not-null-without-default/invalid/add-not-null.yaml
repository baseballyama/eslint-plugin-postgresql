errors:
  - messageId: noAddColumnNotNullWithoutDefault
    message: "`ADD COLUMN ... NOT NULL` without a `DEFAULT` aborts the migration on any table that already has rows. Either supply a `DEFAULT`, or add the column nullable first, backfill, and then `ALTER COLUMN ... SET NOT NULL` in a follow-up."
    line: 1
    column: 30
    endLine: 1
    endColumn: 45
output: null
