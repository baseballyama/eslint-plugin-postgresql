options:
  - style: never
errors:
  - messageId: unexpectedIdentity
    message: Use a serial pseudo-type (e.g. `bigserial`) instead of `GENERATED ... AS IDENTITY`. Useful for projects that need to keep compatibility with tooling that does not understand identity columns.
    line: 1
    column: 17
    endLine: 1
    endColumn: 19
output: null
